# NightwatchJS with Typescript
### Install (windows)
- Clone this project into a new directory
- Enter project directory and Install package dependencies `npm install`
- Use `package.json` scripts to run the tests, for example:
`npm run test-all`